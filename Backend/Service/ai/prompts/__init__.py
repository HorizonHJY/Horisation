"""One module per feature. Each exports a PROMPT_VERSION and a build(...) that
returns the message list. Bump the version on any change of substance — it is
written to every usage row and every reading, so cost, failure rate and
ratings can be compared version against version."""
